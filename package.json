{
  "name": "react-orchestra-web-demo",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.8.4"
  },
  "dependencies": {
    "bulma": "^0.2.3",
    "react": "^15.4.1",
    "react-dom": "^15.4.1",
    "react-orchestra": "^0.1.11"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build && cd build/ && surge --domain react-orchestra.surge.sh ."
  }
}
